package dev.startupstack;

import io.quarkus.test.junit.NativeImageTest;

@NativeImageTest
public class NativeCodeSyncServiceIT extends CodeSyncServiceTest {

    // Execute the same tests but in native mode.
}